export type Document = {
    id: string,
    name: string,
    file_path: string,
    size: number,
    createdAt: string,
}

export type Chat = {
    id: string,
    title: string,
    createdAt: string,
}